 //"use strict"; // new js code will be used

//alert ("hitesh")

let name = "Hitesh"
let age=21
let isLoggedIn= true
console.log(name);

// (bigint) used for bigger numbers like in reddit etc
// (string) 
// (boolean) yes or no OR true/false
// (undefined)
// symbol->unique
//object

console.log(typeof null);// output is object
console.log(typeof undefined);// output is undefined

